import React from "react";

const Projects = () => {
  const projectsInfo = [{}, {}, {}, {}, {}];

  /**ComponentDidMount()
   * - fetch from github api
   * - update state to github response data
   * - useEffects and UseState
   * - inside return map the projectInfo array
   */
  return <div></div>;
};

export default Projects;
